#if !defined(DEFAULTS_HPP)
#define DEFAULTS_HPP

namespace defaults
{
    constexpr int particleNum = 1000;
    constexpr int stepNum = 1000;
    constexpr int saveFreq = 10;
} // namespace defaults

#endif // DEFAULTS_HPP
